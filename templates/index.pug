extends layout.pug 
block content 
    .container
        <section>
                <form class="row g-3">
                    <div class="col-auto">
                        p Estimer 
                    </div>
                    <div class="col-auto">
                        <input type="text" class="form-control" id="city" placeholder="L'adresse">
                    </div>
                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary mb-3">Continuer</button>
                    </div>
                </form>
        </section>
